// First loop

for(let i = 0; i < 10; i++) {
 console.log(i, 'Hello there!')
}

// loop with an array

const mentors = [
    'Ed',
    'Gianna',
    'Mom'
]

for(let i = 0; i < mentors.length; i++) {
    const name = mentors[i]
    console.log(`${name} is an awsome mentor!`)
}